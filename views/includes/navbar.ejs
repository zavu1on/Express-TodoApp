<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="/">Todo App</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <div class="navbar-nav w-100">
            <a class="nav-link <% if (active === 'home') {%>active<%}%>" href="/">Todos</a>
            <% if (isAuth) {%>
                <a class="nav-link <% if (active === 'create') {%>active<%}%>" href="/create">Create</a><%} %>
        </div>
        <% if (!isAuth) {%>
            <a href="/auth/login" class="btn <% if (active === 'login') {%>active<%}%> btn-outline-success mr-3">Login</a>
            <a href="/auth/signup" class="btn <% if (active === 'signup') {%>active<%}%> btn-outline-warning" style="width: 100px">Sign Up</a>
        <%} else {%>
            <a href="/auth/logout/" class="btn btn-outline-danger">Logout</a>
        <% } %>
    </div>
</nav>